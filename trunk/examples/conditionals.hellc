#brains <stdsounds.h>

bloody main() {
  bloody a;
  trap (a > 0) {
    moan("big\n");
  } escape trap (a < 0) {
    moan("small\n");
  } escape {
    moan("null\n");
  }

  fear (bloody i = 0; i < 10; i++) {
    moan("Kiling you for %d-th time\n", i + 1);
  }

  bloody c = 10;
  whimper (c > 2) {
    c -= 3;
  }
}
